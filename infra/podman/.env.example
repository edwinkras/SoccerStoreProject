# Copy this file to .env in the same folder and adjust as needed.
# podman-compose automatically reads a .env file in this directory.

POSTGRES_USER=soccerstore
POSTGRES_PASSWORD=soccerstore_dev_pw
POSTGRES_DB=soccerstore
